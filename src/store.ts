import { createStore } from 'vuex'

export default createStore({
  state() {
      return {
          // color scheme
          colorScheme: '',
          // site
          site: {
              asideAlign: 'right'
          }
      }
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
